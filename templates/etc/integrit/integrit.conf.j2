# {{ ansible_managed | wordwrap(wrapstring='\n# ') }}

# /etc/integrit.conf : configuration file for integrit
#
# See integrit(1) and /usr/share/doc/integrit/examples/
# for more information.
#

root={{ integrit_root }}
known={{ integrit_known_db }}
current={{ integrit_current_db }}

{% for integrit_rule in integrit_rules %}
{{ integrit_rule }}
{% endfor %}
